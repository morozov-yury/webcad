<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
       	http://www.springframework.org/schema/beans 
       	http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

	<!-- MAIN FACTORY -->
	<bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="applicationContextSchedulerContextKey">
			<value>applicationContext</value>
		</property>
		<property name="triggers">
			<list>
				<!-- PUT TRIGGERS HERE -->
				<!-- SAMPLE 
				<ref bean="testCronTrigger" /> 
				-->	
		
			</list>
		</property>
	</bean>

	<!-- SAMPLE TRIGGER AND CRON DEFINITION
	<bean name="testJobDetail" class="org.springframework.scheduling.quartz.JobDetailBean">
		<property name="jobClass" value="diploma.webcad.trigger.TriggerJobBean" />
		<property name="jobDataAsMap">
			<map>
				<entry key="triggerJob" value="testTriggerJob" />				
			</map>
		</property>
	</bean>
	
	<bean id="testCronTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail" ref="testJobDetail" />
		<property name="cronExpression" value="${trigger.cron.time.testCronTrigger}" />
	</bean>
	-->

</beans>